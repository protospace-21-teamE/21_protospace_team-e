.container
  .comment-form
    = form_for @comment, url: {action: 'update'}, html: {id: 'edit_comment'} do |f|
      = f.text_field :comment, name: "comment", placeholder: "コメントする", class: "textbox", value: @comment.comment
      = f.submit "Edit"
